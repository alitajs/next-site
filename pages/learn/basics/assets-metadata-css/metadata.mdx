import Layout from '../../../../components/learn/Layout'
import AnswerBox from '../../../../components/learn/AnswerBox'
import Image from '../../../../components/image'

export const meta = {
  title: '资源、元数据和CSS',
  courseId: 'basics',
  lessonId: 'assets-metadata-css',
  stepId: 'metadata',
  subtitle: '元数据'
}

如果我们想修改页面的元数据，比如 `<title>` HTML 标签？

`<title>`是`<head>` 标签的一部分内容。让我们深入了解如何修改在 Next.js 页面中的 `<head>` 标签。

在编辑器中打开 `pages/index.js` 并查看以下几行:

```jsx
<Head>
  <title>Create Next App</title>
  <link rel="icon" href="/favicon.ico" />
</Head>
```

请注意，使用 `<Head>` 代替小写字母`<head>`。`<Head>` 是内置于 Next.js 中的 React 组件。它允许你修改页面的 `<head>`。

你可以从 [`next/head`](/docs/api-reference/next/head) 模块导入 `Head` 组件。

### 添加 `<Head>` 到 `first-post.js`

我们还没有在我们的 `/posts/first-post` 上增加一个`<title>`，让我们添加一个。

打开 `pages/posts/first-post.js` 文件。

首先，从 [`next/head`](/docs/api-reference/next/head) 导入 `Head`:

```js
import Head from 'next/head'
```

然后，将其添加到 `FirstPost` 组件中。现在，我们只添加 `title` 标签:

```jsx
export default function FirstPost() {
  return (
    <>
      <Head>
        <title>First Post</title>
      </Head>
      …
    </>
  )
}
```

尝试访问 [http://localhost:3000/posts/first-post](http://localhost:3000/posts/first-post)。浏览器标题现在应该是 “First Post”。通过使用浏览器的开发工具，你应该会看到 `title` 标签被添加到 `<head>` 中。

要了解更多关于“Head”组件的信息，请查看【API对“next/Head”的引用】。

> 要了解更多关于 `Head` 组件的信息，请查看 [API 对 `next/head` 的引用](/docs/api-reference/next/head)。
>
> 如果你想自定义 `<html>` ,例如，要添加 `lang` 属性，你可以通过创建一个自定义 `pages/_document.js` 文件来这样做。在[自定义 `Document` 文档](/docs/advanced-features/custom-document)中了解更多信息。

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
