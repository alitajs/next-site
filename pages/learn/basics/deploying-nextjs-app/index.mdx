import Layout from '../../../../components/learn/Layout'
import Link from 'next/link'
import PlataformLink from '../../../../components/name/platform-link'

export const meta = {
  title: '部署你的 Next.js 应用程序',
  courseId: 'basics',
  lessonId: 'deploying-nextjs-app'
}

在这最后的基础课程中，我们将把 Next.js 应用程序部署到生产环境中。

我们将学习如何将 Next.js 部署到 <PlataformLink />，这是由 Next.js 的创建者构建的 Jamstack 部署平台。我们还将讨论其他部署选项。

> **前置条件**: 你需要有一个 [GitHub account](https://github.com/) 来完成本课。

### 这节课你将学到什么

在这一课中，你会学到:

- 如何将 Next.js 应用程序部署到 <PlataformLink />。
- **DPS** 工作流程: **D** 开发、**P** 预览和 **S** 上架。
- 如何将你的 Next.js 应用程序部署到你自己的主机提供商。

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
